-- name: test_mv_rewrite_with_iceberg_stable @slow
function: create_iceberg_catalog("mv_iceberg_${uuid0}", "${iceberg_sql_test_catalog_type}")
-- result:
None
-- !result
set new_planner_optimize_timeout=10000;
-- result:
-- !result
set catalog mv_iceberg_${uuid0};
-- result:
-- !result
create database mv_iceberg_db_${uuid0};
-- result:
-- !result
use mv_iceberg_db_${uuid0};
-- result:
-- !result
CREATE TABLE t1 (
    num int,
    dt string,
    part_id int
)
PARTITION BY (dt, part_id);
-- result:
-- !result
INSERT INTO t1 VALUES
    (1,"2020-06-15", 1),(2,"2020-06-15", 1),
    (2,"2020-06-18", 1),(3,"2020-06-18", 1),
    (3,"2020-06-21", 1),(4,"2020-06-21", 1);
-- result:
-- !result
set catalog default_catalog;
-- result:
-- !result
create database db_${uuid0};
-- result:
-- !result
use db_${uuid0};
-- result:
-- !result
CREATE MATERIALIZED VIEW mv1 PARTITION BY (dt) REFRESH MANUAL
AS
SELECT dt,sum(num) FROM mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 GROUP BY dt;
-- result:
-- !result
[UC]REFRESH MATERIALIZED VIEW mv1 WITH SYNC MODE;
function: print_hit_materialized_view("SELECT dt,sum(num) FROM mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 where dt='2020-06-15' GROUP BY dt;", "mv1")
-- result:
True
-- !result
function: print_hit_materialized_view("SELECT dt,sum(num) FROM mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 GROUP BY dt;", "mv1")
-- result:
True
-- !result
select * from mv1 order by dt;
-- result:
2020-06-15	3
2020-06-18	5
2020-06-21	7
-- !result
SELECT dt,sum(num) FROM mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 where dt='2020-06-15' GROUP BY dt ORDER BY dt;
-- result:
2020-06-15	3
-- !result
SELECT dt,sum(num) FROM mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 GROUP BY dt ORDER BY dt;
-- result:
2020-06-15	3
2020-06-18	5
2020-06-21	7
-- !result
INSERT INTO mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 VALUES
    (1,"2020-06-15", 1),(2,"2020-06-15", 1);
-- result:
-- !result
function: print_hit_materialized_view("SELECT dt,sum(num) FROM mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 where dt='2020-06-15' GROUP BY dt;", "mv1")
-- result:
False
-- !result
function: print_hit_materialized_view("SELECT dt,sum(num) FROM mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 GROUP BY dt;", "mv1")
-- result:
True
-- !result
select * from mv1 order by dt;
-- result:
2020-06-15	3
2020-06-18	5
2020-06-21	7
-- !result
SELECT dt,sum(num) FROM mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 where dt='2020-06-15' GROUP BY dt ORDER BY dt;
-- result:
2020-06-15	6
-- !result
SELECT dt,sum(num) FROM mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 GROUP BY dt ORDER BY dt;
-- result:
2020-06-15	6
2020-06-18	5
2020-06-21	7
-- !result
drop table mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 force;
-- result:
-- !result
drop database mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0} force;
-- result:
-- !result
drop database db_${uuid0} force;
-- result:
-- !result

-- name: test_mv_rewrite_with_iceberg_bugs1_stable @slow
function: create_iceberg_catalog("mv_iceberg_${uuid0}", "${iceberg_sql_test_catalog_type}")
-- result:
None
-- !result
set catalog mv_iceberg_${uuid0};
-- result:
-- !result
create database mv_iceberg_db_${uuid0};
-- result:
-- !result
use mv_iceberg_db_${uuid0};
-- result:
-- !result
CREATE TABLE t1 (
    num int,
    dt varchar(30)
) PARTITION BY (dt);
-- result:
-- !result
INSERT INTO t1 VALUES
    (1,"2020-06-15"),(2,"2020-06-18"),(3,"2020-06-21"),(4,"2020-06-24"),
    (1,"2020-07-02"),(2,"2020-07-05"),(3,"2020-07-08"),(4,"2020-07-11"),
    (1,"2020-07-16"),(2,"2020-07-19"),(3,"2020-07-22"),(4,"2020-07-25"),
    (2,"2020-06-15"),(3,"2020-06-18"),(4,"2020-06-21"),(5,"2020-06-24"),
    (2,"2020-07-02"),(3,"2020-07-05"),(4,"2020-07-08"),(5,"2020-07-11"),
    (2,"2020-07-16"),(3,"2020-07-19"),(4,"2020-07-22"),(5,"2020-07-25");
-- result:
-- !result
set catalog default_catalog;
-- result:
-- !result
create database db_${uuid0};
-- result:
-- !result
use db_${uuid0};
-- result:
-- !result
CREATE MATERIALIZED VIEW mv1 PARTITION BY dt1 REFRESH MANUAL AS
SELECT date_trunc("month",str2date(dt,'%Y-%m-%d')) as dt1,sum(num)
FROM mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 GROUP BY dt1;
-- result:
-- !result
[UC]REFRESH MATERIALIZED VIEW mv1 WITH SYNC MODE;
SELECT date_trunc("month",str2date(dt,'%Y-%m-%d')) as dt1,sum(num)
FROM mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 GROUP BY dt1 order by 1;
-- result:
2020-06-01	24
2020-07-01	48
-- !result
function: print_hit_materialized_view("SELECT date_trunc('month',str2date(dt,'%Y-%m-%d')) as dt1,sum(num) FROM mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 GROUP BY dt1 order by 1;", "mv1")
-- result:
True
-- !result
INSERT INTO mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 VALUES (3,"2020-06-15");
-- result:
-- !result
function: print_hit_materialized_view("SELECT date_trunc('month',str2date(dt,'%Y-%m-%d')) as dt1,sum(num) FROM mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 GROUP BY dt1 order by 1;", "mv1")
-- result:
True
-- !result
SELECT date_trunc("month",str2date(dt,'%Y-%m-%d')) as dt1,sum(num)
FROM mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 GROUP BY dt1 order by 1;
-- result:
2020-06-01	27
2020-07-01	48
-- !result
drop database default_catalog.db_${uuid0} force;
-- result:
-- !result
DROP TABLE mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0}.t1 FORCE;
-- result:
-- !result
drop database mv_iceberg_${uuid0}.mv_iceberg_db_${uuid0} FORCE;
-- result:
-- !result
DROP CATALOG mv_iceberg_${uuid0};
-- result:
-- !result
